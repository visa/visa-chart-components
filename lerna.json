{
  "packages": ["packages/*", "packages/charts-python/js"],
  "version": "independent",
  "npmClient": "yarn",
  "command": {
    "version": {
      "message": "chore(release): publish",
      "ignoreChanges": ["*.md", "*.txt"],
      "allowBranch": ["master", "main", "development"]
    },
    "publish": {
      "conventionalCommits": true
    }
  }
}
